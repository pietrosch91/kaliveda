KALIVEDA_SET_INCLUDE_DIRS(VAMOS MODULES analysis)

BUILD_KALIVEDA_MODULE(identification
	PARENT ${KVSUBPROJECT}
	KVMOD_DEPENDS geometry calibration
	EXTRA_LIBS ${KVIndra_LIB_LIST} ${KVMultiDet_LIB_LIST}
)
