#---set VAMOS specific compiler flags
set(VAMOS_SPECIFIC_FLAGS "-Wextra -Wdeprecated")
message(STATUS "VAMOS specific flags: ${VAMOS_SPECIFIC_FLAGS}")

set(CMAKE_CXX_FLAGS_RELWITHDEBINFO
	"${CMAKE_CXX_FLAGS_RELWITHDEBINFO} ${VAMOS_SPECIFIC_FLAGS}")
set(CMAKE_CXX_FLAGS_RELEASE
	"${CMAKE_CXX_FLAGS_RELEASE} ${VAMOS_SPECIFIC_FLAGS}")
set(CMAKE_CXX_FLAGS_DEBUG
	"${CMAKE_CXX_FLAGS_DEBUG} ${VAMOS_SPECIFIC_FLAGS}")

set(CMAKE_C_FLAGS_RELWITHDEBINFO
	"${CMAKE_C_FLAGS_RELWITHDEBINFO} ${VAMOS_SPECIFIC_FLAGS}")
set(CMAKE_C_FLAGS_RELEASE
	"${CMAKE_C_FLAGS_RELEASE} ${VAMOS_SPECIFIC_FLAGS}")
set(CMAKE_C_FLAGS_DEBUG
	"${CMAKE_C_FLAGS_DEBUG} ${VAMOS_SPECIFIC_FLAGS}")

#---list of datasets
set(datasets VAMOS_e503)

#---set paths to find all KVMultiDet headers
KALIVEDA_SET_INCLUDE_DIRS(KVMultiDet MODULES ${KVMultiDet_MOD_LIST})
#---set paths to find all KVIndra headers
KALIVEDA_SET_INCLUDE_DIRS(KVIndra MODULES ${KVIndra_MOD_LIST})

BUILD_KALIVEDA_SUBPROJECT(DATASETS ${datasets})
