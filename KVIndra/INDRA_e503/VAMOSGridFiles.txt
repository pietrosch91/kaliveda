# Grid files for VAMOS
#
# The name (i.e. VAMOS_SICSI) doesn't matter, it only affects the message
# printed to the terminal at present. We tokenize on the '=' character and
# strip the tokens so you can call it what you like, as long as it doesn't
# contain '='. You can append freely, all grids will be loaded.

VAMOS_SICSI       = grid_vamos.dat
VAMOS_CHIOSI      = grid_chio_si_vamos.dat
VAMOS_SITOF       = grid_si_tof_vamos.dat
VAMOS_CHIO_V2     = grid_chio_v2.dat
VAMOS_SITOF_CODE2 = cuts_sitof_code2.dat
VAMOS_Q_AQ        = grid_Q_AQ.dat
VAMOS_Q_AQ_CHIOSI = grid_Q_AQ_chiosi.dat

